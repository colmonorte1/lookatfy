import { Loading } from '@/components/ui/Status/Status';

export default function LoadingPage() {
  return (
    <main>
      <Loading description="Cargando servicios y expertos" />
    </main>
  );
}
